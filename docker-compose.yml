services:
  umineko-web:
    build: .
    ports:
      - "${PORT:-8080}:80"
    restart: unless-stopped
    volumes:
      - ${GAME_PATH:-./game}:/usr/share/nginx/html/game:ro
      - asset-cache:/usr/share/nginx/html/cache
    environment:
      - HOSTING_MODE=${HOSTING_MODE:-production}
      - SITE_URL=${SITE_URL:-}

volumes:
  asset-cache:
